[INITIAL SETUP]
1-first you need to have an environment variable with the name "resAdminPassword", 
this password will be needed to log in to the app for admin role

2- cd into current directory then run:
python -m resBot.setup

[RUN]
cd into current directory them run:
python run.py